<style>
    .cat-barra { display: flex; flex-wrap: wrap; align-items: center; gap: 12px; padding: 14px 18px; margin-bottom: 18px; }
    .cat-barra form { flex: 1 1 220px; max-width: 300px; }
    .cat-barra .btn-guinda { margin-left: auto; }
    .dif-barra { height: 8px; border-radius: 999px; background: #eef0f3; overflow: hidden; }
    .dif-barra span { display: block; height: 100%; background: var(--guinda); border-radius: 999px; }
    .curso-card { display: flex; flex-direction: column; gap: 10px; height: 100%; color: var(--text); transition: border-color .15s, transform .15s; }
    .curso-card:hover { border-color: var(--guinda-100); color: var(--text); transform: translateY(-2px); }
    .curso-meta { color: var(--muted); font-size: .82rem; }
    .estrellas { color: #d4a017; letter-spacing: 1px; }
    .tag-alta { background: #fde4cf; color: #b45309; }
    .tip-voto { border: 0; background: #f4f5f7; border-radius: 8px; padding: 5px 12px; font-weight: 700; font-size: .85rem; color: #3f3f46; }
    .tip-voto:hover:not(:disabled) { background: #eceef2; }
    .tip-voto.activo { background: var(--guinda-50); color: var(--guinda); box-shadow: inset 0 0 0 1px var(--guinda-100); }
    .tip-link { border: 0; background: none; color: var(--muted-2); font-size: .82rem; padding: 0; }
    .tip-link:hover { color: var(--bad); }
</style>
