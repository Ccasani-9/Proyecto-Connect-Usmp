# Blueprint de Render: despliega la rama main con el Dockerfile de la raíz.
services:
  - type: web
    name: usmpconnect
    runtime: docker
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: Registro__CodigoDocente
        sync: false
